<Project>

  <Target Name="IncludeAllInPackage">
    <ItemGroup>
      <TfmSpecificPackageFile Include="$(OutputPath)\*.*" Exclude="**/$(TargetName).*" PackagePath="$(BuildOutputTargetFolder)/$(TargetFramework)" />
    </ItemGroup>
  </Target>

</Project>